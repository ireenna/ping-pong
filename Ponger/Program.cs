using System;
using RabbitMQ.Client;
using RabbitMQ.Wrapper.Interfaces;
using RabbitMQ.Wrapper.QueueServices;

namespace Ponger
{
    class Program
    {
        static void Main(string[] args)
        {
            //IConnectionFactory a = new ExtendedConnectionFactory();
            //IMessageProducerScopeFactory mp = new MessageProducerScopeFactory(a);
            //IMessageConsumerScopeFactory mc = new MessageConsumerScopeFactory(a);
            //MessageService ms = new MessageService(mp, mc);
            //ms.Run();

        }
    }
}
